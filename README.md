# Dashboard Module

A Laravel module that extends Saucebase core functionality. Designed for seamless integration with the ecosystem.

## Installation

To install the Dashboard module, run the following commands:

```bash
composer require saucebase/dashboard
composer dump-autoload
php artisan module:enable Dashboard
```
